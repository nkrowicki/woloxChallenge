import React from 'react'
import styles from './LoginScreen.module.scss';

const LoginScreen = () => {
    return (
        <div>
            <h1 className={styles.title}>LoginScreen</h1>
        </div>
    )
}

export default LoginScreen
